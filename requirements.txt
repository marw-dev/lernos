# LernOS — Abhängigkeiten
# Installieren: pip install -r requirements.txt
# System-Pakete: sudo apt install poppler-utils  (für Vision-Pipeline)

# Kern
click>=8.0
requests>=2.28
colorama>=0.4

# PDF-Verarbeitung
pdfplumber>=0.9
pdfminer.six>=20221105

# Vision-Pipeline (benötigt: sudo apt install poppler-utils)
Pillow>=9.0
pdf2image>=1.16

# Entwicklung
pytest>=7.0
pytest-cov
